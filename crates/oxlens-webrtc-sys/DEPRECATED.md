# DEPRECATED

이 crate는 **폐기**되었습니다.

## 사유

Android에서 Kotlin + libwebrtc Java API로 전환 (2026-03-14).
livekit webrtc-sys C++ FFI 바인딩이 더 이상 필요하지 않음.

## 대체

`platform/android/oxlens-sdk/` (org.webrtc.* Java API 직접 사용)
